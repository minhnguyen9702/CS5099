# Annotation Viewer

To open the Viewer, you must serve `index.html` over HTTP. 
If you're not sure how to do this, simply run:

```
python serve.py
```

This starts a local server and opens the viewer in your browser.
An internet connection is required to load three.js. Press `Ctrl+C`
to stop the server.
